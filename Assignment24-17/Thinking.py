1、今天讲解了时间序列预测的两种方式，实际上在数据库内建时间属性后，可以产生时序数据库，请思考什么是时序数据库？为什么时间序列数据成为增长最快的数据类型之一？
时序数据是基于时间的一系列的数据。在有时间的坐标中将这些数据点连成线，往过去看可以做成多纬度报表，揭示其趋势性、规律性、异常性;往未来看可以做大数据分析，机器学习，实现预测和预警。
时序数据库就是存放时序数据的数据库，并且需要支持时序数据的快速写入、持久化、多纬度的聚合查询等基本功能。
对比传统数据库仅仅记录了数据的当前值，时序数据库则记录了所有的历史数据。同时时序数据的查询也总是会带上时间作为过滤条件。时序数据常常是一些实时监控数据，他们随着时间生成，并且通常是同时生成不同维度乃至不同模态的数据。随着移动互联网、万物互联技术的发展，
人们主动（撰写微博、发朋友圈等）和被动（步数、地点信息、购买记录等）产生的信息被采集并传输到服务器上保存下来，这些信息随着时间增长并且高并发，往往也会保留历史信息而不是覆盖，
所以会成为增长最快的数据类型之一。
时序数据的特点：
1、数据随着时间增长，根据维度取值，而数据纬度几乎不变。
2、持续高并发写入，设备越多，写入数量越大，而且由于定期采样，写入量平稳。但是几乎不会有更新操作（一个设备在某个时间点产生的数据不会变动）以及单独数据点的删除（通常只会删除过期时间范围内所有的数据）
3、查询一般都是查最近产生的数据，很少会去查询过期的数据。
4、设备之间的数据关联性小，同种类设备A和设备B产生的数据互相并不依赖。你并不需要join。